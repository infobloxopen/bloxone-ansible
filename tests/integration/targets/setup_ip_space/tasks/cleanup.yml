---
- module_defaults:
    group/infoblox.bloxone.all:
      csp_url: "{{ csp_url }}"
      api_key: "{{ api_key }}"
  block:
    - name: "Delete IP space"
      infoblox.bloxone.ipam_ip_space:
          name: "{{ ip_space_name }}"
          state: "absent"
      ignore_errors: true
