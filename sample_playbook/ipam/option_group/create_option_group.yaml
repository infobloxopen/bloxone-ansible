---
- hosts: localhost
  connection: local
  collections:
    - infoblox.b1ddi_modules

  vars:
  - host: "{{ Host server to connect to }}"
  - api: "{{ api key to access the server }}"

  tasks:
    #  Create a Optiongroup
    - name: Create Option Group
      b1_ipam_option_group:
        name: "test"
        tags:
          - "Org": "Infoblox"
          - "Dept": "QA"
        comment: "This is a test Optiongroup to validate Infoblox Ansible Collection"
        host: "{{ host }}"
        api_key: "{{ api }}"
        state: present
        